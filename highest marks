#include<stdio.h>
struct student {
    int marks;
};

int main() {
    struct student s[3];
    int max=0;

    for(int i=0;i<3;i++){
        scanf("%d",&s[i].marks);
        if(s[i].marks > max)
            max = s[i].marks;
    }
    printf("%d", max);
}
