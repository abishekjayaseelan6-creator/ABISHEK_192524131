#include<stdio.h>
struct date {
    int d,m,y;
};

struct student {
    int id;
    struct date dob;
};

int main() {
    struct student s;
    scanf("%d %d %d %d",&s.id,&s.dob.d,&s.dob.m,&s.dob.y);
    printf("%d %d/%d/%d",s.id,s.dob.d,s.dob.m,s.dob.y);
}
