#include<stdio.h>
struct student {
    int id;
};

int main() {
    struct student s[5];
    for(int i=0;i<5;i++)
        scanf("%d",&s[i].id);

    for(int i=0;i<5;i++)
        printf("%d ",s[i].id);
}
