#include<stdio.h>
struct student {
    int id;
};

int main() {
    struct student s[3];
    for(int i=0;i<3;i++)
        scanf("%d",&s[i].id);

    for(int i=0;i<3;i++)
        printf("%d ",s[i].id);
}
